# Features

Artifacts for each feature live in their own folder.
Naming convention: [feature-name]-[number]
Numbers are sequential per feature name.
Do not overwrite a previous version — create a new numbered folder.
